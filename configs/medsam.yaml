data:
  img_dir: /root/autodl-tmp/Glas/Image
  lbl_dir: /root/autodl-tmp/Glas/Label
  img_size: 1024
  batch_size: 1
  val_batch_size: 1
  num_workers: 2
  n_samples: null

model:
  type: medsam
  checkpoint: weights/medsam_vit_b.pth

train:
  epochs: 200
  lr: 0.0001
  encoder_lr_scale: 0.1
  batch_size: 1
  save_dir: results/medsam
